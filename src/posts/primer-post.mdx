---
title: Por qué debería seguir aprendiendo estructuras de datos y algoritmos
description: Con el surgimiento generalizado de la inteligencia artificial, muchos ahora asumen que hay menos razones paratos y algoritmos es muy útil.
slug: primer-post
published: true
publishedOn: '26 julio 2023'
estimatedReadingTime: 5 min
---

React es una biblioteca JavaScript de código abierto que ha revolucionado el desarrollo de aplicaciones web. Creada por Facebook, React permite construir interfaces de usuario interactivas y dinámicas de manera eficiente y sencilla. En este breve post, exploraremos los conceptos básicos de React y por qué ha ganado tanta popularidad entre los desarrolladores.

## My h2Title

Here is an image:

There is a code snippet below:
```tsx
export function myComponent() {
    return (
        <p>test</p>
    )
}
```

CSS:

```css
:root {
  --background-color: hsl(0, 0%, 0%);
  --color: hsl(0, 0%, 100%);
  --gray-color:#a4adb6;
}

::-webkit-scrollbar {
  width: 1.6rem;
}

::-webkit-scrollbar-thumb {
  background-color: var(--gray-color);
  border-radius: 1rem;
  border: .4rem solid transparent;
  background-clip: content-box;
}

::-webkit-scrollbar-thumb:hover {
  background-color: #c7c7c799;
}

::selection {
  background-color: var(--color);
  color: var(--background-color);
}

.radialGradientBackground {
  position: fixed;
  left: 0;
  z-index: -1;
  width: 100%;
  height: 100%;
  background-image: radial-gradient(farthest-side at 50% 50%, #2a2a2a, transparent);
}

.bodyContainer{
  max-width: 160rem;
  margin-inline: auto;
  padding-inline: 2rem;
}

.mainContainer{
  max-width: 120rem;
  margin-inline: auto;
}
```
